# Secrets
.env
config/affiliation.local.json

# Dépendances (le générateur n'en a pas, mais au cas où)
node_modules/

# Sorties générées (on garde uniquement les exemples versionnés)
output/*
!output/.gitkeep
!output/exemple-*

# Données opérationnelles locales
data/revenue.json
data/clicks.json

# Système
.DS_Store
*.log
